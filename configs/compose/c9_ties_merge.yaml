id: C9
description: Stack(merged_LA_DA, TA)  - TIES weight-merge (Yadav et al. 2023)
merge_method: ties     # 'ties' | 'linear'
adapters:
  LA_swh: ${ckpt_dir}/la_swh
  DA_eng: ${ckpt_dir}/da_eng_bio_on_base
  TA:     ${ckpt_dir}/ta_mcqa/seed${run.seed}/TA
